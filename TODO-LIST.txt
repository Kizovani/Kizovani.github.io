HOME PAGE:

1. make the hover to reveal thing on the main menu look nice please,
IDEAS:
make the dot smaller and make it grow on hover? and then click to reveal? And then click another dot to hide it again?
old small dot hover idea?

2. 
// CODE HERE TO DETECT BROWSER: You can add message like I can see you are on [Browser Here] , 
// depending on browser you can be like: "this one doesnt work well, I have no idea why" please forward complaitns to [dev email for that browser here]


function detectBrowser() {
  const userAgent = navigator.userAgent;
  let browserName;

  if (userAgent.match(/chrome|chromium|crios/i)) {
    browserName = "Chrome";
  } else if (userAgent.match(/firefox|fxios/i)) {
    browserName = "Firefox";
  } else if (userAgent.match(/safari/i)) {
    browserName = "Safari";
  } else if (userAgent.match(/opr\//i)) {
    browserName = "Opera";
  } else if (userAgent.match(/edg/i)) {
    browserName = "Edge";
  } else if (userAgent.match(/trident/i)) {
    browserName = "Internet Explorer";
  } else {
    browserName = "Unknown";
  }

  return browserName;
}

console.log("Browser: " + detectBrowser());